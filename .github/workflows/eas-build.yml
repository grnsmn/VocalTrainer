name: EAS Build & Submit
on:
  push:
    branches:
      - main
      - features/eas-workflow

jobs:
  build:
    name: Install and Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24.x
          cache: npm

      - name: Setup Expo
        uses: expo/expo-github-action@v8
        with:
          token: ${{ secrets.EXPO_TOKEN }}
          eas-version: latest

      - name: Install dependencies
        run: npm ci

      - name: Create Service Account JSON
        env:
          GOOGLE_SERVICE_ACCOUNT: ${{ secrets.GOOGLE_SERVICE_ACCOUNT }}
        run: node -e "require('fs').writeFileSync('google-services-key.json', process.env.GOOGLE_SERVICE_ACCOUNT)"

      - name: Build and Submit to Google Play
        run: eas build --platform android --profile production --non-interactive --auto-submit --local
